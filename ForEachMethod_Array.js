// To iterate in Arrays with ForEach method 

let nums = [2,1, 3, 4, 19, 33, 3];

nums.forEach((i, index , list)=>{   // it will print all the values of the arrays 
    console.log("values: "+i +", index: "+ index +", Array: "+ list);
})